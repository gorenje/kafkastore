<h1>Bad Clicks - can't be stored</h1>

<form action="/badclicks/clear_all" method="post">
   Delete all?
   <input name="confirm" type="text" value="if you type 'yes' here, i will pleasure your requirements" size=80/>
   <input type="submit"/>
</form>
<p>

The Clicks themselves:

<table border=1>
  <thead>
    <tr>
      <% @keys.each do |key| %>
        <th><%= key %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @elements.each do |e| %>
      <tr>
        <% @keys.each do |key| %>
          <td><%= e[key] %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
